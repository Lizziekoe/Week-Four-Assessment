<h3>Add a Band that Will Play at This Venue</h3>
<form action="/bands" method="post">
  <input type="text" name="band">
  <button type="submit">Add</button>
</form>

<% @bands.each do |band| %>
<a href="/bands/<%= band.id%>"><%= band.name %></a> <br>
<% end %>
